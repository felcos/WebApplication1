@model WebApplication1.Models.TblTipoCentro

@{
    ViewData["Title"] = "Crear Nuevo";
}

<h1>Crear Nuevo</h1>

<h4>Tipo de Centros</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TipoCentro" class="control-label"></label>
                <input asp-for="TipoCentro" class="form-control" />
                <span asp-validation-for="TipoCentro" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Regresar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
